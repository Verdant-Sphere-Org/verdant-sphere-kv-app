name: CI Workflow

# trigger
on:
  pull_request:
    branches:
      - dev
      - main

jobs:
  build:
    uses: ./.github/workflows/build.yaml

  test:
    uses: ./.github/workflows/build.yaml

  scan:
    uses: ./.github/workflows/scan.yaml

