name: QA Workflow

# trigger
on:
  push:
   branches:
    - features/qa

jobs:
  build:
    uses: ./.github/workflows/build.yaml

  test:
    needs: [build]
    uses: ./.github/workflows/test.yaml

  scan:
    uses: ./.github/workflows/scan.yaml