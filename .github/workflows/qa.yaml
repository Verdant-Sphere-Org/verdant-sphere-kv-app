name: QA Workflow

# trigger
on:
  push:
   branches:
    - features/qa

jobs:
  build:
    uses: ./.github/workflows/build.yaml

  test:
    uses: ./.github/workflows/build.yaml

  scan:
    uses: ./.github/workflows/scan.yaml